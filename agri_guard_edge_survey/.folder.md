# Folder: /agri_guard_edge_survey

1. **地位 (Position)**: 航测/探查类任务的**边缘侧 CLI**：在本地 GPU/MPS/CPU 上跑与中心一致的检测与聚类流水线，原图不落库、不上传。
2. **逻辑 (Logic)**: `survey process` → SQLite + 证据/L2；`survey ping` / `sync --dry-run` 联调预检；`survey sync`（可选密码登录选云任务、`--target-mission-id`；**`sync_target_mission_id` 锁定首次云目标**、`--reset-sync-target`；分片 **`edge_session_*` 每批落库**、状态文件或 DB 续传、`COMPLETED` 自动对齐本地；**无续传 session 且 manifest 含 `target_mission_id` 时先 POST `import/repair-from-storage`（OSS 已有则只做写库，409 再走路 staging）**；**成功 sync 后打印「同步统计」并 `GET /missions/{id}` 拉云端合计**→ `build_import_bundle` + POST import；无子命令时 `interactive_menu`。
3. **约束 (Constraints)**: 不直连 PostgreSQL；原图不上传；同步需中心已部署 `edge_survey` 路由；代码注释英文；交互菜单子命令用 `cli.main` 以保证终端进度可见；**菜单项 5 sync 非 dry-run 自动加 `--chunked`（可走 repair-from-storage）**，可选 `--resume`；`LocalDatabase.connect()` 必须使用 `finally: conn.close()`（原生 `sqlite3` 的 `with conn` 不会关连接，大批量会 EMFILE）。**本地安装**：`pyproject.toml` 将 `agri-guard-core` 声明为 `file:./agri_guard_core`，`pip` 按**当前工作目录**解析；请在 **仓库根**执行 `pip install -e ./agri_guard_edge_survey`（勿在仅有 `agri_guard_edge_survey/` 的子目录裸跑 `pip install .`，除非你另执行 `pip install -e ../agri_guard_core`）。
